# Mini Image Gallery


A simple full-stack app for uploading single images (JPEG/PNG), stored in backend memory, displayed in a gallery.


## Run Backend


```bash
cd backend
npm install
npm run dev # or npm start
